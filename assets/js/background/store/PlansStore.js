var PlansStore = function(config) {
  config = config || {};

  $.extend(this, {
  		
  }, config);

  Plans
 Store.superclass.constructor.call(this, config);
}

PlansStore.prototype = {
	/**
	 * 
	 */
	add: function() {

	}
}
extend(Plans
Store, Observable);